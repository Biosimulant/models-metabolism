schema_version: '2.0'
title: 'Metabolism: Central Carbon (COMBO_0020)'
description: "Metabolism Central Carbon (COMBO_0020) organizes best_et_1, beste2007_genome_2, state_compare_mon into a comparative simulation setup to examine how central carbon mechanisms compare across these models. When you run it, you can compare participant trajectories through shared runtime settings and routed output streams."
models:
- repo: Biosimulant/models-metabolism
  alias: best_et_1
  manifest_path: models/metabolism-sbml-best-et-al-2024-metabolic-metaorganism-model-of-model2310020001-model/model.yaml
  parameters:
    model_path: data/MODEL2310020001.xml
- repo: Biosimulant/models-metabolism
  alias: beste2007_genome_2
  manifest_path: models/metabolism-sbml-beste2007-genome-scale-metabolic-network-of-myco-model1507180021-model/model.yaml
  parameters:
    model_path: data/MODEL1507180021.xml
- repo: Biosimulant/models-observability
  alias: state_compare_mon
  manifest_path: models/observability-state-comparison-monitor/model.yaml
  parameters:
    max_points: 2000
    min_dt: 1.0
- repo: Biosimulant/models-observability
  alias: state_metrics_mon
  manifest_path: models/observability-state-metrics-monitor/model.yaml
  parameters:
    max_points: 2000
    min_dt: 1.0
runtime:
  duration: 60.0
  tick_dt: 0.01
  initial_inputs: {}
wiring:
- from: best_et_1.state
  to:
  - state_compare_mon.state_a
  - state_metrics_mon.state_a
- from: beste2007_genome_2.state
  to:
  - state_compare_mon.state_b
  - state_metrics_mon.state_b
scientific_context:
  question: How do central carbon mechanisms compare across these models?
  mode: comparative
  assumptions:
  - Model implementations are used as published in their curated manifests without
    biological reinterpretation.
  - Time integration uses a shared global tick compatible with model min_dt constraints.
  - Comparative (non-causal) mode is used because full deterministic IO coverage for
    causal coupling was not satisfied.
  expected_observables:
  - Model output trajectories under shared runtime settings.
  - Cross-model agreement/divergence in key state or metric signals.
  - Relative behavior comparison without causal linkage claims.
  limitations:
  - No new biology is introduced beyond what upstream models encode.
  - Cross-model semantic matching is rule-based and may under-connect uncertain routes.
  provenance:
  - 'metabolism-sbml-best-et-al-2024-metabolic-metaorganism-model-of-model2310020001-model
    :: biomodels_ebi:MODEL2310020001 :: https://www.ebi.ac.uk/biomodels/MODEL2310020001'
  - 'metabolism-sbml-beste2007-genome-scale-metabolic-network-of-myco-model1507180021-model
    :: biomodels_ebi:MODEL1507180021 :: https://www.ebi.ac.uk/biomodels/MODEL1507180021'
  visualization_rationale: A dedicated monitor model receives all participating model
    state streams (`state_a..state_d`) so trajectories can be compared in one place
    without claiming causal coupling when IO semantics are incomplete.
